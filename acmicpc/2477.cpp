#include <iostream>
#include <set>
#include <vector>
#include <map>
#include <list>
#include <unordered_map>
#include <string>
#include <algorithm>
using namespace std;

int main()
{
    ios::sync_with_stdio(false);
    cin.tie(NULL);

    int n;
    int board[20][20], cnt = 0;
    cin >> n;
    for (int i = 0; i < n; i++) // 각 행마다 들어가는 퀸 i
    {
        /* code */
    }

    cout << cnt;
}

// https://www.acmicpc.net/problem/9663